Let's say I have a local project that was not cloned and have to be merged with git repo.

-> git init
-> git remote add origin <remote_repo_url>
-> git fetch origin
-> git merge origin/master
-> git commit -m "Merge local changes into remote repository"
-> git push origin master

